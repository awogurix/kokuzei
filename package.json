{
  "name": "nami-navi",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir dist && cp -r api/ components/ services/ *.html *.tsx *.ts *.json dist/"
  },
  "dependencies": {
    "@google/genai": "^1.19.0"
  }
}